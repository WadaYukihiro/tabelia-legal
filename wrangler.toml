name = "tabelia-legal"
compatibility_date = "2026-05-28"
compatibility_flags = ["nodejs_compat"]
workers_dev = true

[observability]
enabled = true

[assets]
directory = "."
# 存在しないURLでは /404.html を404ステータスで返す（レシピslug変更時の旧URL等）
not_found_handling = "404-page"
